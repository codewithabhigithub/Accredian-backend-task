datasource db {
  provider = "mysql"
  url      = "mysql://root:Abhi@123@localhost:3306/refer"
}

generator client {
  provider = "prisma-client-js"
}

model Referral {
  id             Int      @id @default(autoincrement())
  name           String
  email          String
  referralMessage String
  createdAt      DateTime @default(now())
}
